# Security Policy

Do not file public issues for vulnerabilities. Use GitHub private vulnerability reporting with reproduction steps and impact.

ConfigCraft has no backend and does not request real secrets. Generated secrets are placeholders. Users remain responsible for secret management, TLS, digest pinning, cluster policies, access controls, availability, and reviewing output. Only the latest default-branch version receives fixes.
