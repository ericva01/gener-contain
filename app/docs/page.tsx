import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = { title: "Documentation | ConfigCraft", description: "Learn how to generate and use ConfigCraft infrastructure files." };

const outputs = [
  ["Dockerfile", "A production-minded, multi-stage container image for Next.js or Spring Boot."],
  ["Docker Compose", "Runs the application, optional PostgreSQL database, and optional Nginx proxy together."],
  ["Environment example", "Documents safe placeholder variables without generating or storing real secrets."],
  ["Nginx", "A reverse-proxy baseline connected to the selected application port."],
  ["GitHub Actions", "A CI workflow that installs dependencies, checks the project, and builds it."],
  ["Kubernetes", "Deployment, Service, namespace, optional ConfigMap, Secret template, and PostgreSQL storage."],
  ["Publish shell script", "Builds and pushes your Docker image, then commits and pushes the current Git branch."],
];

export default function DocsPage() { return <main><section className="docs-hero"><div><p className="eyebrow">Documentation</p><h1>Generate with confidence.<br/><em>Ship with context.</em></h1><p>Everything you need to move from a ConfigCraft selection to files running in your project.</p></div><nav aria-label="On this page"><span>On this page</span><a href="#quick-start">Quick start</a><a href="#outputs">Generated files</a><a href="#publish">Publish script</a><a href="#security">Security</a></nav></section><div className="docs-layout"><aside><span>Documentation</span><a href="#quick-start">Quick start</a><a href="#outputs">Generated files</a><a href="#publish">Docker & Git publishing</a><a href="#security">Security notes</a><Link href="/generator">Open generator →</Link></aside><article className="docs-content"><section id="quick-start"><p className="eyebrow">01 · Quick start</p><h2>Create your configuration</h2><ol><li><b>Choose a preset.</b> Start with the closest application and deployment shape.</li><li><b>Set your runtime.</b> Select the framework, version, ports, and database.</li><li><b>Choose output files.</b> ConfigCraft updates the preview immediately.</li><li><b>Review and download.</b> Copy one file or download the complete ZIP.</li></ol><div className="docs-note"><b>Private by default</b><span>Generation happens entirely in your browser. ConfigCraft does not upload project names, settings, or generated files.</span></div></section><section id="outputs"><p className="eyebrow">02 · Outputs</p><h2>What each file does</h2><div className="docs-output-grid">{outputs.map(([title, text]) => <div key={title}><h3>{title}</h3><p>{text}</p></div>)}</div></section><section id="publish"><p className="eyebrow">03 · Automation</p><h2>Publish to Docker Hub and Git</h2><p>Select <b>Dockerfile</b> and <b>Publish shell script</b> in the generator. After downloading, make the script executable and run it from the project root:</p><pre><code>{`chmod +x scripts/publish.sh\nDOCKERHUB_USERNAME=your-user \\\nDOCKERHUB_TOKEN=your-token \\\n./scripts/publish.sh v1.0.0`}</code></pre><p>The tag argument is optional and defaults to <code>latest</code>. The image name defaults to your ConfigCraft project name. Override it with <code>IMAGE_NAME</code>, or the Git branch with <code>GIT_BRANCH</code>.</p><div className="docs-warning"><b>Before running it</b><span>The script stages all current Git changes with <code>git add --all</code>. Review <code>git status</code> first, and use a Docker Hub access token rather than your account password.</span></div></section><section id="security"><p className="eyebrow">04 · Security</p><h2>Keep secrets out of Git</h2><p>Generated environment and Kubernetes secret files contain placeholders only. Replace them through your deployment platform or secret manager, and never commit real credentials. Review image tags, TLS, access policies, probes, and resource limits for your environment before production use.</p></section></article></div></main>; }
